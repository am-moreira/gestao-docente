# Copie o conteúdo deste arquivo para um novo arquivo chamado .env na raiz do projeto.
# Não envie o .env ao GitHub.

# Cole a URL completa da aba "Connection String" do TiDB Cloud.
DATABASE_URL=mysql://USUARIO:SENHA@HOST:4000/sys

# Acesso automático apenas no ambiente local iniciado com pnpm dev.
LOCAL_AUTH_ENABLED=true
LOCAL_ADMIN_NAME=Administrador Local
LOCAL_ADMIN_EMAIL=admin@local.test

# Gere uma chave local: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=troque-por-uma-chave-local-aleatoria
